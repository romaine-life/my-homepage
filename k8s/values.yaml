# The image tag is bumped by .github/workflows/build-and-deploy.* on every
# push to main (via `sed` on this file).
image:
  repository: romainecr.azurecr.io/my-homepage
  # Quoted on purpose: a bare SHA like 4255e97 is valid YAML scientific notation; keep the quotes.
  tag: "baac74c"
