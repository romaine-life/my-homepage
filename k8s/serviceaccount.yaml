# Static-only pod — no Azure SDK calls — so workload identity isn't strictly
# needed. Keeping the ServiceAccount for pattern consistency with other apps;
# it's harmless if unused.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: infra-shared
  namespace: my-homepage
