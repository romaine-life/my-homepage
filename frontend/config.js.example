// fztApiBase points at the fzt-frontend service that hosts /fzt/tree/:id.
// homepageVersion is the short SHA of the build commit (or "dev" locally).
// Both overridden at generate-config.sh time.
export const CONFIG = {
  fztApiBase: "https://fzt-frontend.romaine.life",
  homepageVersion: "dev",
};
