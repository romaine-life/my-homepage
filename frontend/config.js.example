// fztApiBase points at the fzt-frontend service that hosts /fzt/tree/:id.
// Override at generate-config.sh time for staging/other envs.
export const CONFIG = {
  fztApiBase: "https://fzt-frontend.romaine.life",
};
